# API Configuration for Live Streaming
# The URL of the BPT Service (Redis Streams API)
# Leave empty to use Vite proxy (default for development)
# Set to full URL for production deployment
# Default: '' (uses Vite proxy to localhost:3000)
VITE_API_URL=

# API Key for authentication (optional)
# If the service is protected with an API_KEY, set it here
VITE_API_KEY=

# Mock Sensors Configuration
# Set to 'true' to use mock data (random power/cadence/HR)
# Set to 'false' to use real Bluetooth sensors
# Default: 'true' in development, 'false' in production
VITE_USE_MOCK_SENSORS=
