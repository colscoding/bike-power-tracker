{
    "name": "service",
    "version": "1.0.0",
    "description": "Redis Streams service with Node.js, Express, and real-time messaging",
    "main": "src/server.js",
    "scripts": {
        "start": "node src/server.js",
        "dev": "nodemon src/server.js",
        "dev:docker": "docker-compose up -d redis && nodemon src/server.js",
        "test": "mocha tests/**/*.test.js --timeout 10000 --exit",
        "test:unit": "mocha tests/unit.test.js --exit",
        "test:integration": "mocha tests/redis.test.js --timeout 10000 --exit",
        "test:api": "mocha tests/api.test.js --timeout 10000 --exit",
        "test:coverage": "nyc npm test",
        "test:watch": "nodemon --exec 'npm test'",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "keywords": [
        "redis",
        "streams",
        "nodejs",
        "express",
        "real-time",
        "sse",
        "server-sent-events"
    ],
    "author": "",
    "license": "MIT",
    "engines": {
        "node": ">=14.0.0",
        "npm": ">=6.0.0"
    },
    "dependencies": {
        "express": "^4.18.2",
        "express-rate-limit": "^8.2.1",
        "helmet": "^8.1.0",
        "redis": "^4.6.7"
    },
    "devDependencies": {
        "chai": "^4.3.10",
        "eslint": "^8.55.0",
        "mocha": "^10.2.0",
        "nodemon": "^3.0.1",
        "nyc": "^15.1.0",
        "prettier": "^3.1.1",
        "supertest": "^6.3.3"
    }
}
