# Bike Power Tracker - Environment Configuration
# Copy this file to .env and fill in your values

# ==============================================
# SERVER CONFIGURATION
# ==============================================

# Server port
PORT=3000

# Node environment (development, production)
NODE_ENV=development

# CORS configuration (comma-separated origins or * for all)
CORS_ORIGIN=*

# Optional API key for authentication (leave empty to disable)
API_KEY=

# ==============================================
# REDIS CONFIGURATION
# ==============================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ==============================================
# DATABASE CONFIGURATION (Optional)
# ==============================================
# The database is optional - if not configured, the service
# will work with Redis only (no workout history persistence)

# Database provider: postgresql or sqlite
DATABASE_PROVIDER=postgresql

# PostgreSQL connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://biketracker:your-password-here@localhost:5432/biketracker?schema=public

# Or for SQLite (simpler, for single-user/home deployments):
# DATABASE_PROVIDER=sqlite
# DATABASE_URL=file:./data/biketracker.db

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=biketracker
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=biketracker
